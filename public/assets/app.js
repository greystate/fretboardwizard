// Generated by CoffeeScript 1.10.0
(function() {
  var Chord, FretWizController, ref;

  this.app = (ref = window.app) != null ? ref : {};

  FretWizController = (function() {
    function FretWizController() {
      this.setupThing();
    }

    FretWizController.prototype.setupThing = function() {};

    return FretWizController;

  })();

  if (typeof $ === "function") {
    $(function() {
      return app.controller = new FretWizController;
    });
  }

  Chord = (function() {
    var DEFAULT_OPTIONS;

    DEFAULT_OPTIONS = {
      name: "Major triad",
      formula: "135"
    };

    function Chord(options) {
      if (options == null) {
        options = DEFAULT_OPTIONS;
      }
      this.name = options.name, this.formula = options.formula;
    }

    return Chord;

  })();

  window.Chord = Chord;

}).call(this);
